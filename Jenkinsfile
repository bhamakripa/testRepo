pipeline {
  agent none
  stages {
    stage('Initialise') {
      steps {
        echo 'first step initialise'
        echo '2nd print statement within initialise'
      }
    }
    stage('finish') {
      steps {
        sh 'echo "kripa finish"'
      }
    }
  }
}